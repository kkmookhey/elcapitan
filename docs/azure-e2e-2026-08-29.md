# Authorized Azure end-to-end evidence — 2026-08-29

This record covers a synthetic, non-customer run in the disposable El Capitan
lab. It is evidence for the tested deployment and two existing Azure Storage
action connectors, not broader Azure execution capability.

## Boundary

- Tenant: `017c6f31-f951-4bda-a50a-c168c0e6f815`
- Subscription: `Azure CIS Agent Testing`
  (`8cd2b4cc-c789-466d-a8f7-8f51fb20985d`)
- Resource group: `elcapitan-remediation-lab-rg`
- Exact mutation target:
  `/subscriptions/8cd2b4cc-c789-466d-a8f7-8f51fb20985d/resourceGroups/elcapitan-remediation-lab-rg/providers/Microsoft.Storage/storageAccounts/elcapassure8f51fb20985d`
- Candidate source commit: `57cfcb5b5fcd5ea38e6e918c89ace83cd8ff1378`
- Runtime image digest:
  `sha256:06262644e76bbbc93932456f8eed11c81cfcd337d3f4794c1caf2558d5d3df1e`
- Azure worker image digest:
  `sha256:7a68a519090d59e17d13cc7209058e58a246ee25d37220de8770ec614e8f6eea`

The run used only the checked-in synthetic fixture
`examples/azure-lab-e2e-intake.json`. It made no model call, accessed no
customer data, and did not read, alter, or assign new permissions to Eiger.

## Identity separation

The fresh shadow scanner identity had `Reader` only at the named resource
group. The fresh action identity had the checked-in
`El Capitan Storage Security Remediator - Lab` role only at the exact Storage
account. That role contains only `Microsoft.Storage/storageAccounts/read` and
`Microsoft.Storage/storageAccounts/write`, with no data actions or key-list
permission. The pre-existing pull identity retained only its ACR pull role.

A dedicated scanner-identity probe attempted
`Microsoft.Storage/storageAccounts/write`. Azure returned
`AuthorizationFailed`, and the probe confirmed that `publicNetworkAccess`
remained `Enabled` and `allowBlobPublicAccess` remained `true`.

## Hosted shadow result

The current image ran as `elcapitan-e2e-shadow` in the existing private
Container Apps environment against a new isolated PostgreSQL database.

- `/healthz` returned `status: ok`, with PostgreSQL state and artifact stores
  and zero pre-existing artifacts.
- Unauthenticated API access returned `401`; a valid token login returned
  `303`.
- Connector status reported managed-identity mode, 35 Azure validators, no
  missing environment fields, and no mutation credential or endpoint.
- Intake submitted and received two findings, created one exact-resource case,
  attached the second finding, and reported zero duplicates.
- Case `CASE-954927504995429754` advanced to `validated` at version 3.
- Validation record `VAL-760697298342600297` and minimized evidence record
  `EVD-606727600214677244` confirmed both live findings: public network access
  was `Enabled` and anonymous Blob access was allowed.
- The promotion manifest was ready while approval, scheduling, execution, and
  external-model capabilities remained absent from the shadow service.

An in-app browser was not connected during the run, so visual UI acceptance
was not executed. The authenticated HTTP/API path and returned UI data were
exercised directly; this record does not convert that into screenshot evidence.

## Action-plane result

The dedicated non-root Azure worker authenticated with the action identity in
an isolated Azure CLI configuration directory. Both executable controls passed
their rollback and success scenarios against the exact target:

| Control | Requested outcome | Result | Live before/after | Typed verification |
|---|---|---|---|---|
| Public network access | rollback | `rolled_back` | `Enabled` → `Enabled` | `VERIFY-251292262713980839`; checkpoint restored and service recovered |
| Public network access | success | `remediated` | `Enabled` → `Disabled` | `VERIFY-425756535103136957`; post-change probe passed |
| Anonymous Blob access | rollback | `rolled_back` | `true` → `true` | `VERIFY-556098030552349843`; checkpoint restored and service recovered |
| Anonymous Blob access | success | `remediated` | `true` → `false` | `VERIFY-792690176246080159`; post-change probe passed |

The two success runs produced accepted release audits, remediation
certificates, and originator handoffs. No provider was configured, so the
release review used the recorded local reviewer and made no model call.

## Restoration and cleanup

After verification, the action identity restored the exact original Storage
configuration: `publicNetworkAccess: Enabled` and
`allowBlobPublicAccess: true`. Independent read-only checks then confirmed:

- no resource containing `elcapitan-e2e` remained in the resource group;
- no role assignment remained for either temporary identity;
- both temporary ACR repositories and manifests were absent;
- the isolated PostgreSQL database and role were deleted;
- the shared PostgreSQL server was `Ready` with public access `Disabled`;
- both temporary Keychain entries were absent; and
- the assurance Storage account retained its original tags and properties.

The temporary runtime consisted of three ACR builds, one short-lived Container
App, and bounded manual Container Apps job executions on existing lab
infrastructure. Azure cost data had not settled when cleanup completed, so this
record does not claim an exact charge; the run introduced no persistent billed
resource and stayed within the authorized $50 incremental budget by design.

The completed repository slice subsequently passed all 540 tests, the release
tree and shell-syntax checks, and wheel/source-distribution builds.
